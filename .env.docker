# ===========================================
# PORTS (Unique ports to avoid conflicts)
# ===========================================
APP_PORT=9987      # Application port (external)
DB_PORT=9988       # PostgreSQL port (external)

# ===========================================
# DATABASE (Local PostgreSQL in Docker)
# ===========================================
DB_PASSWORD=your_secure_password_here
DATABASE_URL=postgresql://postgres:your_secure_password_here@postgres:5432/gas_bridge_hub

# Note: For Docker, SUPABASE_* variables are not used
# They're kept for compatibility with the codebase
SUPABASE_URL=http://postgres:5432
SUPABASE_ANON_KEY=local-development-key
SUPABASE_SERVICE_KEY=local-development-key

# ===========================================
# AUTHENTICATION
# ===========================================
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-key-minimum-32-characters-long-please-change-this

# Your application URL (change for production)
BETTER_AUTH_URL=http://localhost:9987

# ===========================================
# APPLICATION
# ===========================================
FRONTEND_URL=http://localhost:9987
PORT=3000
NODE_ENV=production

# ===========================================
# OPTIONAL: Production Settings
# ===========================================
# For production deployment, update these:
# BETTER_AUTH_URL=https://your-domain.com
# FRONTEND_URL=https://your-domain.com
